modLoader="javafml"
loaderVersion="[1,)"
license="${mod_license}"
issueTrackerURL="https://github.com/knoxhack/Echo/issues"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
displayURL="https://github.com/knoxhack/Echo"
authors="KnoxHack"
credits="Shared packet architecture, sync helpers, and safe optional network bridges for ECHO addons."
description='''
ECHO: NetCore provides the shared networking, payload registration, sync, server action,
rate limiting, debug packet logging, and optional addon packet bridge for the ECHO ecosystem.
'''

[[dependencies.${mod_id}]]
modId="neoforge"
type="required"
versionRange="[${neo_version},)"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId="minecraft"
type="required"
versionRange="${minecraft_version_range}"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId="echocore"
type="required"
reason="Provides shared ECHO service contracts and no-op fallback behavior."
versionRange="[1.0.0,)"
ordering="AFTER"
side="BOTH"
