# Native Rollback Guide

Keep the previous module artifact, descriptor, checksum, and pack manifest before replacing a native module.

If validation fails, restore the previous artifact and retry only the failed module after the release metadata is corrected.
